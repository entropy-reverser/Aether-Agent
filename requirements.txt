# Runtime dependencies (pin major.minor; allow patch via Dependabot).
# Only direct imports are listed here — no transitive deps.
fastapi==0.133.0
uvicorn==0.41.0
pydantic==2.13.3
pydantic-settings==2.14.0
loguru==0.7.2
pyyaml==6.0.2
